export const APIClientCredentials = {
  host: "https://example.com/fitbit",
  base64UserPassword: 'xxxxxxx', // Header auth if applied on server is base64 format. convert user:password to base64 format
};
